"use strict"
let greeting;
greetign = {}; // Typo!
console.log(greetign);

/* let greeting;
greetign = {}; // Typo!
console.log(greetign); // {} */

/**
 * JS is very forgiving in nature so even if we have not added let, const, or var it will be added automatically
 * But if we use "use strict" this will throw an error
 */